# AlphaLab 历史截面因子研究与组合回测 Spec

## Problem Statement

AlphaLab 已经具备 ETF 策略、行情仓库、组合构建、回测和本地 Dashboard 等基础能力，但还缺少一条专门服务于股票因子研发的、可复现的完整研究闭环。

当前用户希望在历史上的任意一个时点，只使用该时点及之前能够看到的数据运行一组可迭代的选股因子；根据因子完成过滤、打分和排序；选出 Top N 股票构建组合；再从该时点向后模拟一个月、两个月或其他指定周期，检查组合收益、回撤和相对基准表现。这个过程不是无人值守的自动因子挖掘，而是由用户和 Codex 共同编写、审阅并迭代 Python 因子插件的人机协作研发流程。

用户还需要一个轻量的、类似 TradingView 的人工审阅界面。因子运行之后，用户需要能够按排名逐只浏览候选池股票，查看选股时点之前的 K 线、成交量、因子值、通过或落选原因及组合权重，从而发现规则与直觉不一致、数据异常、极端样本和错误入选。前端的职责仅限研究审阅；因子编辑、实验编排和批量运行优先通过 Python 与命令行完成。

现有系统不能直接可靠地满足这一需求：ETF 策略核心绑定了 ETF 专用标的池与固定评分逻辑；股票 Dashboard 的评分和展示逻辑集中在现有 Dashboard 后端中；当前股票 universe 主要表示当前或近期状态，不是完整的历史时点 universe；不同市场和数据源还存在复权口径差异。如果直接在现有 Dashboard 或回测器中继续叠加规则，会造成未来数据泄漏、研究逻辑重复、实验不可追溯，以及无法公平比较不同因子版本。

## Solution

新增一个独立的“历史截面研究”深模块，以一次不可变的“实验运行”为核心。调用方提交实验规格，其中包含历史日期、单一市场、可投资域策略、因子插件及参数、评分聚合方式、Top N 组合策略、交易成本、观察周期和基准。研究模块负责解析有效信号日、构建严格截至该日的历史截面、运行因子、生成可审计排名、构建目标组合、在下一可交易日开盘建仓、向后模拟指定交易日数，并输出候选池、组合、净值路径、收益与回撤报告。

因子以受版本管理的本地 Python 插件形式挂载。插件只负责过滤与打分，不负责持仓权重、交易撮合、未来收益计算、数据下载或结果持久化。每次修改因子都形成新的插件版本或源码哈希，并产生新的实验运行，从而可以比较迭代前后的结果而不覆盖历史证据。

第一阶段以命令行为主，打通“历史截面 → 因子 → 排序 → Top N → Portfolio → 前瞻模拟 → 实验产物”的最短闭环。第二阶段增加只读的本地候选池审阅页：左侧为可筛选、可排序的候选列表，右侧为 K 线和成交量图，支持上一只、下一只和键盘导航。审阅模式默认只显示有效信号日及之前的行情；未来行情和组合表现只能在单独的评估模式中显式打开，避免人工调因子时无意使用未来信息。

同一研究接口支持一个或多个历史截面。单日期实验提供描述性结果；多日期研究用同一因子和同一组合策略重复运行，汇总超额收益分布、胜率、样本量和置信区间。系统不得根据单个历史日期声称因子具有统计显著优势。

## User Stories

1. 作为因子研究者，我希望选择任意一个历史日期，从而复原当时能够进行选股的研究场景。
2. 作为因子研究者，我希望系统记录我输入的日期和最终解析出的有效信号日，从而知道周末、节假日或缺失行情是如何处理的。
3. 作为因子研究者，我希望选股规则只能看到有效信号日及之前的数据，从而避免未来函数污染实验。
4. 作为因子研究者，我希望为一次实验选择一个市场，从而避免在初版中混入跨市场交易日历、时区和汇率问题。
5. 作为因子研究者，我希望选择历史可投资域策略，从而控制哪些股票有资格进入筛选流程。
6. 作为因子研究者，我希望看到可投资域的总股票数和每层过滤后的数量，从而定位候选池骤减发生在哪一步。
7. 作为因子研究者，我希望系统检查每只股票是否有足够的历史数据，从而避免短历史样本产生失真的指标。
8. 作为因子研究者，我希望系统检查数据复权口径，从而确保因子计算与未来收益比较使用一致的价格序列。
9. 作为因子研究者，我希望用一个独立的 Python 模块实现一个因子，从而可以与 Codex 快速沟通并迭代规则。
10. 作为因子研究者，我希望每个因子具有稳定的 ID、版本和参数，从而可以精确复现实验。
11. 作为因子研究者，我希望因子声明所需字段和最小历史窗口，从而让系统只加载必要数据并在运行前验证依赖。
12. 作为因子研究者，我希望一个因子可以充当硬过滤条件，从而剔除明确不符合要求的股票。
13. 作为因子研究者，我希望一个因子可以输出连续分数，从而对通过基础条件的股票进行排序。
14. 作为因子研究者，我希望一个因子可以同时输出通过状态和分数，从而表达先过滤再择优的复合规则。
15. 作为因子研究者，我希望为每只股票保存因子的原始值、标准化值和原因码，从而能够解释排名。
16. 作为因子研究者，我希望多个硬过滤条件默认全部通过才入选，从而得到明确且可预测的组合语义。
17. 作为因子研究者，我希望多个评分因子使用统一的横截面标准化和显式权重，从而公平合成总分。
18. 作为因子研究者，我希望指定分数方向，从而正确处理“越大越好”和“越小越好”的指标。
19. 作为因子研究者，我希望查看全部候选排名，而不仅是最终 Top N，从而理解入选边界附近的股票。
20. 作为因子研究者，我希望为组合指定 Top N，从而快速比较集中和分散的持仓方案。
21. 作为因子研究者，我希望默认构建等权组合，从而让初版结果主要反映选股质量而不是复杂权重优化。
22. 作为因子研究者，我希望可选按分数加权，从而测试排名强度是否能转化为仓位差异。
23. 作为因子研究者，我希望限制单只股票和单一行业的最大权重，从而避免结果由少量集中暴露主导。
24. 作为因子研究者，我希望当入选股票少于最低数量时看到明确状态，从而不把样本不足误当成有效组合。
25. 作为因子研究者，我希望空候选池成为合法实验结果，从而可以审阅规则为什么没有选出股票。
26. 作为因子研究者，我希望组合在信号日后的下一可交易日开盘建仓，从而避免使用选股时尚未可成交的价格。
27. 作为因子研究者，我希望指定21、42或其他交易日观察周期，从而评估约一个月和两个月的表现。
28. 作为因子研究者，我希望系统按每日收盘价生成组合净值路径，从而计算期间收益和最大回撤。
29. 作为因子研究者，我希望配置佣金与滑点，从而避免结果完全忽略基本交易成本。
30. 作为因子研究者，我希望看到每只持仓的收益和组合贡献，从而定位组合好坏的来源。
31. 作为因子研究者，我希望把组合与同一历史可投资域的等权基准比较，从而判断因子是否优于随机持有可选股票。
32. 作为因子研究者，我希望可选指定一个市场基准标的，从而比较组合与常用指数的表现。
33. 作为因子研究者，我希望看到组合相对基准的超额收益和回撤差异，从而判断优势是否值得承担风险。
34. 作为因子研究者，我希望在未来数据不足时仍保留选股结果，并把相应观察周期标记为不可评估，从而不会因数据截止而丢失研究产物。
35. 作为因子研究者，我希望在多个历史日期重复同一实验，从而评估因子是否只在某一天有效。
36. 作为因子研究者，我希望看到多日期超额收益的均值、中位数、胜率、样本数和95%置信区间，从而判断证据强度。
37. 作为因子研究者，我希望样本不足或观察窗口高度重叠时系统拒绝给出“显著优势”结论，从而避免夸大结果。
38. 作为因子研究者，我希望每次运行生成独立的运行 ID，从而可以引用、比较和重新打开历史实验。
39. 作为因子研究者，我希望每次运行记录因子源码哈希、配置哈希、数据快照和代码版本，从而复现实验。
40. 作为因子研究者，我希望重新运行相同实验时不会静默覆盖旧结果，从而保留完整迭代轨迹。
41. 作为因子研究者，我希望通过命令行运行单日期实验，从而快速打通和迭代基本流程。
42. 作为因子研究者，我希望通过命令行运行多日期研究，从而无需在网页中配置批量任务。
43. 作为因子研究者，我希望通过命令行列出、查看和比较实验运行，从而管理因子迭代结果。
44. 作为因子研究者，我希望通过命令行打开某次实验的本地审阅页，从而直接进入人工检查。
45. 作为因子研究者，我希望在审阅页看到候选股票的总排名、各因子值、是否进入组合及目标权重，从而把图形和规则结果对照起来。
46. 作为因子研究者，我希望在审阅页按最终入选、落选、行业和原因码过滤股票，从而聚焦可疑样本。
47. 作为因子研究者，我希望在审阅页按排名逐只查看股票，从而快速进行人工模式识别。
48. 作为因子研究者，我希望通过上一只、下一只按钮和键盘快捷键切换股票，从而减少逐只审阅的操作成本。
49. 作为因子研究者，我希望看到日线 K 线和成交量，并在图上标出有效信号日，从而理解因子触发时的走势上下文。
50. 作为因子研究者，我希望审阅模式默认不显示信号日后的 K 线，从而避免未来走势影响对因子合理性的判断。
51. 作为因子研究者，我希望通过显式切换进入评估模式后查看未来窗口，从而对照选股后的真实表现。
52. 作为因子研究者，我希望评估模式在图上标出建仓日和各观察周期终点，从而理解回测收益的价格来源。
53. 作为因子研究者，我希望查看组合净值和回撤曲线，从而快速判断收益是否伴随不可接受的路径风险。
54. 作为因子研究者，我希望审阅页只读取已冻结的实验结果，从而保证浏览行为不会改变候选池或回测结果。
55. 作为开发者，我希望用内存行情适配器和固定因子插件运行完整实验，从而在不依赖大型本地数据库的情况下验证外部行为。
56. 作为开发者，我希望插件输出经过模式和数值检查，从而在异常分数、重复股票或缺失字段进入组合前失败。
57. 作为开发者，我希望插件异常使实验明确失败并保存诊断信息，从而避免静默剔除股票造成错误结论。
58. 作为开发者，我希望行情查询按因子声明的最大回看窗口下推到 DuckDB，从而避免每次实验加载全市场五年数据。
59. 作为开发者，我希望研究模块与模拟交易账本保持分离，从而避免研究运行污染账户事实记录。
60. 作为开发者，我希望未来网页或其他入口只能调用同一个研究接口，从而不复制选股与回测逻辑。

## Implementation Decisions

- 新增独立的历史截面研究模块，不把核心逻辑加入现有 ETF 策略、模拟交易账本或 Dashboard 请求处理代码。
- 历史截面研究模块的唯一业务外部接缝为 `HistoricalResearchLab.run(ExperimentSpec) -> ExperimentReport`。CLI、批量研究和未来其他入口均通过这个接口运行实验。
- 采用以下领域术语：
  - “请求日期”是用户输入的历史日期。
  - “有效信号日”是该市场在请求日期当日或之前最近一个具有完整收盘行情的交易日。
  - “历史截面”是有效信号日时可见的可投资域、元数据和行情集合。
  - “因子插件”是对历史截面执行过滤或打分的受版本管理 Python 模块。
  - “候选池”是通过硬过滤并具有有效评分的股票集合。
  - “目标组合”是从候选池中根据组合策略选出的股票及权重。
  - “前瞻窗口”是从建仓日开始、仅供评估模块使用的未来行情区间。
  - “实验运行”是一次不可变的历史截面选择、组合构建和前瞻评估结果。
  - “研究”是使用同一实验规格在多个历史截面上的重复运行与汇总。
- `ExperimentSpec` 至少包含请求日期或请求日期列表、市场、可投资域策略、因子插件引用与参数、评分聚合配置、组合配置、执行成本、观察周期、基准、数据质量模式和随机种子。
- `ExperimentReport` 至少包含运行身份、请求日期与有效信号日、数据质量摘要、可投资域漏斗、逐股票因子结果、完整排名、目标组合、建仓信息、逐日净值、逐观察周期指标、基准比较、诊断信息和可复现性元数据。
- V1 每次实验只支持一个市场和一种基础货币。实现接口保持市场无关，但首个验收数据路径聚焦 A 股日线。港股和美股在各自复权与历史 universe 数据质量满足相同契约后接入。跨市场组合、汇率换算和跨时区撮合不进入 V1。
- 请求日期为非交易日时，默认解析为此前最近一个完整交易日；报告同时展示请求日期和有效信号日，不静默替换。
- 行情数据模块提供两个内部能力：构建严格截至有效信号日的只读历史截面，以及在目标组合冻结后加载前瞻窗口。因子插件与组合构建模块只能接收历史截面，不能取得前瞻窗口。
- DuckDB 行情适配器根据市场、股票集合、日线周期和因子声明的最大历史窗口执行查询下推。实验运行期间只读固定本地数据，不触发网络请求、行情更新或合成数据回退。
- 提供内存行情适配器作为测试 adapter。生产 DuckDB adapter 与测试内存 adapter 共同证明数据 seam 是真实的，而非只有一个实现的假抽象。
- V1 支持两种可投资域质量模式：
  - `observed-history`：从有效信号日前已经出现、具有足够历史记录且满足数据质量条件的股票构建 universe，允许快速打通现有数据，但报告必须标记未完全消除幸存者偏差。
  - `point-in-time`：要求具有生效区间的历史标的状态和分类数据，用于能够产生统计结论的正式研究。
- 历史标的状态需要支持上市日期、退市日期、状态生效区间和行业分类生效区间。只有当前状态而无历史生效区间时，不得把研究标记为完全 point-in-time。
- 数据质量检查至少覆盖价格非正值、OHLC 关系、重复日线、缺失率、最小历史长度、成交量或成交额可用性、复权标志、复权方法和未来窗口完整性。
- 同一实验中的股票必须使用兼容的复权口径。严格模式下发现混合复权或未知复权即终止实验；探索模式可以剔除不兼容股票，但必须记录数量、股票和原因。
- 因子插件是可信的本地 Python 代码，通过显式 registry 或实验配置挂载，不扫描任意目录并自动执行未知文件。
- 因子插件声明唯一 ID、语义版本、角色、支持市场、必需字段、最小历史交易日数、分数方向和可配置参数模式。
- 因子插件获得不可变的历史截面上下文，并返回逐股票结果。结果包含股票身份、通过状态、原始分数、可选诊断值和稳定原因码。
- 因子插件不得访问网络、数据库连接、系统当前时间、研究结果存储、目标组合或未来行情。相同输入、版本和参数必须产生相同输出。
- 插件 registry 拒绝重复 ID 与版本、缺失声明、不受支持市场和参数模式错误。插件执行异常、输出重复股票、输出 universe 外股票、无穷数或非法类型会使该实验运行失败，不进行静默修复。
- 规则聚合先对所有硬过滤条件执行逻辑 AND，再对通过者进行评分。无硬过滤插件时，数据质量合格的历史 universe 全部进入评分阶段。
- 连续评分默认在每个历史截面内转换为百分位分数，再按显式权重合成总分。缺失评分的处理策略必须由配置声明，默认剔除而非填零。
- 所有排名使用确定性的并列处理与稳定二级排序键。相同输入必须得到相同候选顺序。
- 目标组合默认选择总分最高的 Top N 并等权持有。可选评分加权，但不在 V1 引入均值方差、风险平价或机器学习权重优化。
- 组合约束支持单股票权重上限、单行业权重上限、最低入选数量和最大持仓数。约束导致股票被跳过时必须保留原因。
- 空候选池是成功但不可投资的实验结果；目标组合为空，前瞻指标为不可用，不抛出通用系统错误。
- 使用有效信号日收盘后产生的结果，默认在下一可交易日开盘建仓。不得使用有效信号日收盘价作为可成交买入价。
- V1 使用买入并持有的固定观察窗口，不在窗口内执行再平衡、止损、止盈或因子退出。这样可以优先隔离和衡量选股因子的前瞻质量。
- 默认观察周期为21和42个市场交易日，并允许显式添加其他交易日周期。每个周期使用对应交易日收盘价估值；整个持有期按每日收盘生成组合净值与回撤路径。
- V1 支持固定佣金与滑点配置。交易成本应用于组合建仓和观察周期终点的模拟退出，并在无成本与有成本指标中明确区分。
- 基准默认包含同一历史可投资域的等权组合，保证比较对象具有相同的市场与数据可用范围。若配置了市场基准标的且数据完整，则同时输出相对该基准的结果。
- 单日期实验只输出描述性收益、最大回撤、波动、持仓胜率、贡献和超额收益，不输出统计显著性结论。
- 多日期研究对每个历史截面冻结相同因子版本、参数和组合策略。汇总输出样本量、均值、中位数、胜率、分位数和配对超额收益的95% bootstrap 置信区间。
- “存在正向证据”至少要求20个可评估且有效独立的历史窗口、平均超额收益置信区间下界大于零、point-in-time universe 质量通过，并且不存在未披露的数据口径降级。未满足时只报告统计量，不给出显著优势标签。
- 当用户指定的历史日期造成观察窗口重叠时，报告必须披露样本依赖性。V1 的正式证据模式默认选取按最长观察周期互不重叠的截面；重叠样本仅作为探索结果。
- 每个实验运行生成不可变运行 ID。结果存储与模拟交易账本分开，至少保存 manifest、因子结果表、候选排名、组合、净值曲线、指标、诊断和 Markdown 摘要。
- manifest 记录实验规格哈希、因子 ID 与版本、因子源码哈希、配置哈希、数据快照身份、数据区间、代码提交；当仓库没有提交时使用相关源码文件哈希代替代码提交并标记状态。
- 相同实验规格可以重新执行以验证确定性，但不得覆盖既有运行。内容相同时报告关联关系，内容不同时产生新的独立运行。
- CLI 第一阶段提供运行单日期实验、运行多日期研究、列出运行、显示运行摘要、比较两个运行和打开审阅页的能力。
- CLI 的正常输出聚焦运行 ID、有效信号日、universe 漏斗、Top N、组合指标、数据质量警告和产物位置；完整逐股票结果写入实验产物，不全部刷到终端。
- 本地候选池审阅页由独立的只读研究展示模块提供。它读取已冻结实验产物与对应只读行情，不调用因子插件、不重新计算排名、不修改组合。
- 审阅页的最小布局为候选列表与单股票图表双栏。候选列表展示排名、股票、行业、总分、主要因子值、通过状态、是否入选和目标权重；图表展示日线蜡烛图、成交量和有效信号日标记。
- 审阅页支持按入选状态、行业和原因码过滤，支持排序、文本搜索、上一只、下一只和键盘导航。V1 不要求复杂多图联动或自定义画线工具。
- 审阅模式默认只请求和显示有效信号日及之前的行情。用户必须显式切换到评估模式后，前端才请求未来行情并显示建仓日、观察周期终点和收益。
- 评估模式展示单股票未来路径、组合净值、组合回撤、持仓贡献和基准对照。视觉上必须明确区分“选股时可见区间”和“事后评估区间”。
- 复用现有项目中 Lightweight Charts 的蜡烛图与成交量交互先例，但研究页拥有独立的展示状态和后端查询模块；不得继续扩大现有 Dashboard 单文件中的业务职责。
- 前端不提供因子源码编辑器、拖拽式规则构建器、参数优化器或实验调度器。因子和实验规格仍通过 Python 与配置文件迭代。
- 实现顺序固定为：先打通单日期 CLI 研究闭环及实验产物；再完成候选池人工审阅页；随后增加多日期研究、运行比较和正式证据判断。

## Testing Decisions

- 测试只断言外部可观察行为，不检查私有函数调用、内部 DataFrame 中间列、SQL 文本或模块组织。只要接口结果、错误模式和不变量保持一致，内部实现重构不应导致测试修改。
- 业务验收使用一个主要接缝：向 `HistoricalResearchLab.run` 提交实验规格，通过内存行情 adapter 和固定因子插件获得完整 `ExperimentReport`。测试从请求日期一路覆盖历史截面、因子结果、排名、Top N、权重、下一交易日开盘建仓、未来净值、收益、回撤和诊断。
- 展示验收使用一个独立但只读的高层接缝：从已保存的固定实验产物启动本地审阅页，通过浏览器验证候选排序、股票切换、图表数据、有效信号日标记和未来数据默认隐藏。展示测试不重新运行因子，也不复测业务计算。
- 核心未来数据防护测试构造在信号日前后均有明显异常价格的行情，验证插件输入和候选排名完全不受信号日后数据影响，同时验证评估模块可以在组合冻结后使用未来数据。
- 日期解析测试覆盖正常交易日、周末、市场节假日、信号日数据不完整、请求日期早于数据起点和请求日期晚于数据终点。
- universe 测试覆盖信号日后上市股票、信号日前退市股票、当前 universe 与历史状态冲突、历史状态缺失及 `observed-history` 降级标记。
- 数据质量测试覆盖重复日线、OHLC 非法关系、非正价格、缺失成交量、历史长度不足、混合复权、未知复权和前瞻窗口不足。
- 插件契约测试覆盖合法过滤插件、合法评分插件、复合插件、参数错误、重复插件 ID、不支持市场、插件异常、重复股票输出、universe 外输出、NaN、无穷数和不稳定输出。
- 规则聚合测试覆盖多个硬过滤条件、正向与反向评分、百分位标准化、权重合成、缺失分数策略、并列分数和稳定排序。
- 组合测试覆盖 Top N、等权、评分加权、单股票上限、行业上限、最低数量、边界排名、空候选池和权重和不超过100%的不变量。
- 前瞻评估测试使用可以手工计算的短价格序列，验证下一交易日开盘成交、21/42交易日终值、交易成本、每日净值、最大回撤、持仓贡献和基准超额收益。
- 未来数据不足测试验证选股结果仍被保存，而缺失的观察周期具有明确的 `INSUFFICIENT_FORWARD_DATA` 状态，不使用较短窗口冒充完整结果。
- 确定性测试对相同历史截面、插件版本、参数和随机种子运行两次，验证候选排名、组合、净值和内容哈希一致，同时运行记录互不覆盖。
- 多日期研究测试覆盖独立窗口、重叠窗口、少于最小样本量、置信区间跨零、置信区间完全大于零和 universe 质量降级，确保系统只在全部证据条件满足时给出正向证据标签。
- 实验存储测试覆盖完整产物、失败运行诊断、运行列表、运行读取、运行比较和旧运行不可变。
- 浏览器测试验证默认审阅模式不返回或渲染有效信号日之后的蜡烛；切换到评估模式后才显示建仓日与未来窗口。
- 浏览器测试验证候选列表选择、上一只、下一只、键盘导航、过滤、搜索和排名信息与固定实验产物一致。
- 大型真实 DuckDB 只用于少量本地 smoke test 和性能基准，不作为常规测试套件的必需依赖。常规测试必须可以在小型内存数据上离线运行。
- 性能验收记录一次 A 股全市场单截面实验的查询行数、峰值内存和总耗时，确保查询只覆盖因子声明的必要窗口，而不是加载完整五年全市场数据。具体性能阈值在实现计划中依据本机基线确定。
- 现有先例包括：AlphaLab 的 prepare→execute→reconcile→report 全链路集成测试、回测与回放一致性测试、固定合成数据黄金样本、ETF 横截面评分与候选池测试，以及 DuckDB 行情幂等写入与读取测试。
- 新业务接缝测试建立后，不为每个内部编排步骤重复添加同样断言。纯数学函数或 adapter 契约只有在错误定位价值明显时保留更低层测试。

## Out of Scope

- 自动生成因子、自动修改因子代码或无人值守寻找“最佳因子”。
- 网页中的因子源码编辑、无代码规则搭建、参数拖拽和实验编排。
- 大规模网格搜索、贝叶斯优化、遗传算法或机器学习 AutoML。
- 根据同一批回测结果自动挑选并部署因子。
- 实盘交易、Broker 对接、下单、账户管理、持仓同步和 QMT 集成。
- 将研究组合直接写入现有模拟交易账本。
- V1 中的盘中因子、分钟级撮合和逐笔数据研究。
- V1 中的跨市场组合、外汇换算、跨市场交易日历同步和全球资产统一结算。
- V1 中观察窗口内的动态再平衡、止损、止盈、因子退出和复杂成交模型。
- 均值方差、风险平价、Black-Litterman 等高级组合优化。
- 完整复制 TradingView，包括画线、指标脚本编辑、社交分享、多图布局和云端同步。
- 在研究页中修改实验结果或从页面重新运行因子。
- 对不可信第三方 Python 插件进行操作系统级沙箱、资源隔离或安全审查。
- 在实验运行过程中自动联网下载、修复或更新行情。
- 在缺少历史生效数据时假装已经彻底消除幸存者偏差、行业分类回看偏差或基本面披露时点偏差。
- 基于少数历史截面或高度重叠窗口给出投资有效性承诺。
- 把统计证据解释为未来收益保证或实际投资建议。

## Further Notes

- 当前本地股票日线仓库约覆盖15,237只 A 股、港股和美股股票，数据区间约为2021-01-04至2026-06-25。靠近数据末端的历史截面无法完整评估42交易日窗口，必须显示不可评估状态。
- 当前 A 股日线主要使用前复权数据，但仍存在少量未复权来源；港股和美股在不同本地数据库中的复权状态也不完全一致。数据口径检查是研究闭环的一部分，不是可延后的清理工作。
- 当前 `market_universe` 主要保存当前或近期状态与当前行业分类，没有完整的历史生效区间。因此 `observed-history` 模式适合快速研发和视觉检查，但正式统计证据需要补齐 point-in-time universe。
- “因子”在本规格中专指用于过滤或横截面排序的选股规则，不等同于包含仓位、调仓、退出和执行的完整交易策略。
- 用户与 Codex 的推荐协作方式是：讨论一个因子假设；新增或修改一个 Python 因子插件及其契约测试；运行若干历史截面；先在审阅模式人工检查入选与边界样本；再查看前瞻结果；最后决定保留、修改或淘汰该版本。
- 每次因子迭代都必须保留实验记录。反复查看历史结果后修改规则会产生多重尝试偏差；运行历史应作为研发日志保留，不能只保留最终表现最好的版本。
- 第一交付切片应使用一个简单、可手工验证的示例因子完成单日期全链路，而不是先迁移所有现有 ETF 评分逻辑。
- 第二交付切片完成候选池人工审阅页，优先保证排名、因子解释、K 线、成交量、信号日边界和快速逐只导航。
- 第三交付切片再完成多日期研究、运行比较和证据标签，避免在单日期闭环尚未可信时过早建设统计层。
- 项目当前没有 Git remote，连接的 GitHub issue tracker 也没有可见仓库。该规格已经在本地落盘，但发布 issue 和添加 `ready-for-agent` 标签仍需要明确的 issue tracker 仓库。
