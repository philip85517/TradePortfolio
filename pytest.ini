[pytest]
# 说明：本机 conda Python 3.13 的 readline 扩展在 macOS 上导入即段错误，
# 而 pytest 的 capture/debugging 插件会间接导入 readline。
# 因此禁用这三个插件；在其他正常环境可删除 addopts 行以恢复完整功能。
addopts = -p no:capture -p no:debugging -p no:cacheprovider
testpaths = alphalab/tests

